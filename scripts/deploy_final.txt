option batch continue
option confirm off
open sftp://su373050:2k26Schnitzel1!@access-5019484682.webspace-host.com/ -hostkey=*
cd /bio_beats/

# Create client directory if not exists
mkdir client

# Synchronize dist to client
# Use -delete to remove old files
synchronize remote -delete -permissions=644 -criteria=size -filemask="| .htaccess" dist ./client

# Upload .htaccess to root
put -resume -preservetime -permissions=644 dist\.htaccess ./.htaccess

exit
